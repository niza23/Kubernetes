apiVersion: v1
kind: Pod
metadata:
  name: nginx
spec:
  containers:
    - name: nginx
      image: nginx:1.14.2
      commands : ["/bin/sh", "-c", "echo hello"]
      
 # no need to always right args, you can add those either as an array (in square bracket: []) or as multiline in yaml      